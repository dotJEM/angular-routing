NG_DOCS={
  "sections": {
    "api": "API Documentation"
  },
  "pages": [
    {
      "section": "api",
      "id": "dotjem.routing",
      "shortName": "routing",
      "type": "overview",
      "keywords": "and angular api apps based deeplinking directives dotjem for module overview provides routing services state that"
    },
    {
      "section": "api",
      "id": "dotjem.routing.$resolve",
      "shortName": "$resolve",
      "type": "object",
      "keywords": "$injector $q $resolve an api array cache clear clears dotjem for from in into is key keys list method object of push pushes remove resolve resolver resolvers routing service states the to used value values"
    },
    {
      "section": "api",
      "id": "dotjem.routing.$route",
      "shortName": "$route",
      "type": "object",
      "keywords": "$location $route $routeparams $routeprovider about all an and api args array as browser can causes change changed changes configured contains controller creates current deep-linking define definition details directive dotjem entry even existing false fill for format formats from generated given hasn have history if into is it jemview map method new ng object of otherwise parameter parameters params path provided reference reinstantiates reload replace result route routes routing scope service should states that the through to tries true url urls used watches with you"
    },
    {
      "section": "api",
      "id": "dotjem.routing.$routeProvider",
      "shortName": "$routeProvider",
      "type": "object",
      "keywords": "$location $route $routechangesuccess $routeparams $routeprovider $routeupdate action activated adds after agains against all allows an and any api applying are assigned be before between bracket brackets broadcasted browser by called calling can case cerverter change changes characters colon configuring contain contains convert converter converters curly current custom decorate decorating decorator default defined definition digits doesn dotjem event example existing expected expression expressions extracted false followed following folowing for from function further given groups if ignorecase in information input insensitive is it just mach mapping match matchcase matched matches matching method missing more name named new next ng no num number numeric numerical object of on one only option or other otherwise overwrites parameter parameters params path performed precent properties raised redirection redirectto redundant regex registered registering regular reload return root route routeprovider routes routing scope search see self sensitive service set sets should slash specific specify starting state still stored string syntax templateurl that the then this through to trailing trigger turns two under up update url used using value values when which will with would"
    },
    {
      "section": "api",
      "id": "dotjem.routing.$scroll",
      "shortName": "$scroll",
      "type": "function",
      "keywords": "$anchorscroll $injector $rootscope $scroll $window after allow also and api as be been being call called current dependant direct directive dotjem element elements enhancements for function has is it jemanchor like loaded name named no number of offers on or otherwise over parameter part performs re-routed register replacement returning routing scroll scrolling serves service target targets that the this to transition when which will with work"
    },
    {
      "section": "api",
      "id": "dotjem.routing.$state",
      "shortName": "$state",
      "type": "object",
      "keywords": "$injector $location $node $q $rootscope $route $scroll $state $stateprovider $statetransition $view above activate activated active address against all allows already also an and another any api are as associated at bar be browser build but by call called can cases change check checks child childparam contains current currently define defined defines defining demonstrate directly does dotjem down either error errors etc example examples expression false finally first flow following for force from full generate generating get gochild gohome goto has have here hierachy home homeparam however if implicitly important in include included index inspired instead is isn it last leaf leave loaded location lookup matches mathces means method must name need next node not object of on only optionally or otherwise out overflow param parameters params parent particular path paths possible previous provided redefined reference relative reload reloaded root route routing say select selected selection selector selectors selects self separator set sibling specify specifying state states stats subset such supports syntax system that the their then these they this those through throw thrown to transition true under url use used using valid views we when where will wish with without won would xpath you"
    },
    {
      "section": "api",
      "id": "dotjem.routing.$stateProvider",
      "shortName": "$stateProvider",
      "type": "object",
      "keywords": "$route $routeprovider $scope $stateprovider $template about above activated add added adds all alone also always an and angular api are areas arranged as assets associate associated at basic be becomes been before but can child children complex concept config configuration configure configuring constraint content controller controllers convention could couldn defined definition demo dependent detail details diliberately directives directly do doesn dotjem each element enhancements even example existing flat following for from full fullname function get given good had handlers has have here hierachy hierarchy hint home how however html if ilustated image important in independendly indicate information into is it just level levels like list load loaded located main many may merely method module more multiple name named names naming nested new no not now number object of og on one onenter onexit original other over page parameters parent phones phonesctrl phonesdetailsctrl phoneslistctrl png possible properties property provide provider provides quite really recents recommended register registered registrations relation reload reloaded reloadonsearch render result root route routing same scroll scrollto search see self separate seperate service should simply single so src staff standing state states static structure suddenly tablets tabletsctrl target template templates that the then there they this those to two type ui under unique unless until updated use used useful using value very view views way ways we well when where will wish with within would you"
    },
    {
      "section": "api",
      "id": "dotjem.routing.$stateTransition",
      "shortName": "$stateTransition",
      "type": "object",
      "keywords": "$statetransition $statetransitionprovider api configure details dotjem for how object on routing see to transitions"
    },
    {
      "section": "api",
      "id": "dotjem.routing.$stateTransitionProvider",
      "shortName": "$stateTransitionProvider",
      "type": "object",
      "keywords": "$statechangeerror $statechangestart $statechangesuccess $stateprovider $statetransitionprovider $transition about above addition after all allows also an and angular another any api are arrays as at basic basically be before below besides between boils book both but by called can cancel canceling cases closed combinations complete config configuration configure configured configuring console could creates define defined defining demo demonstrated dependencies descendant describes destination door dotjem down each either entering etc event example existing exists flow following for from function general get gets global handle handler handlers have here hierarchy how however if in information injectable instead invalid invoke is it item its just leave leaving like list lock locked log many maped match matchers method methods might module more most multiple must name not number object occurs of on one onenter onexit only open opened opening optional or out paper parameter part passed pen perform performed possible properties register registered registering rerouting resolve resolved right root routing see shorthand should simple single so source specialized specific specified stage stages started starts state states steps stp target targeted targeting that the these they this through to transition transitioning transitions under unlocked use used using views wan was way ways we when where wildcard wildcards wildcart will wished write"
    },
    {
      "section": "api",
      "id": "dotjem.routing.$template",
      "shortName": "$template",
      "type": "object",
      "keywords": "$http $injector $q $template $templatecache above actual all an and api are as based be cached defines dotjem either encounters first fn following function get html if ignored is it load location method more must object of on one or order piece promise promises properties raw reprecenting resolves rest retrieves returned returning returns routing service services specifying string template templates than that the those to url used using will"
    },
    {
      "section": "api",
      "id": "dotjem.routing.$view",
      "shortName": "$view",
      "type": "object",
      "keywords": "$q $rootscope $state $statetransitionprovider $template $view $viewrefresh able accessing actually all allows already also an and another any api applied applying are args arguments as be been before beginupdate but by call calling can cancel cases causes change changes clear cleared clears comitted commit completion configuration configurations confroller containing context controller create current currently data define defined dependencies details different directive dom done dotjem each either enables etc event fall flag following for force forces function get gets has if ignored in indicating information injected instead into is it jemview known list locals map marked may means method module more name named namved new not object objects of on one only optional or outside previous primarily progress properties provided raised record referenced reflected refresh refreshed refreshes returned routing run same scope see send series service services set setifabsent setorupdate sets should starts state sticky template templates than that the them then these they this through to transaction transactional transition transitions type under untill update updated updates used value version view views was way ways we when whenever where which will with within won yet"
    },
    {
      "section": "api",
      "id": "dotjem.routing.directive:jemAnchor",
      "shortName": "jemAnchor",
      "type": "directive",
      "keywords": "$scroll an anchor any api directive dotjem eca for identifier jemanchor of point provides routing service the to use"
    },
    {
      "section": "api",
      "id": "dotjem.routing.directive:jemView",
      "shortName": "jemView",
      "type": "directive",
      "keywords": "$state according and animate animation animations api away bring browser by changes complements concurrently configuration content current directive dotjem eca enter every existing file html included including index into is it jemview layout leave main name new occur of overview rendered route routing service state template that the time to used view with"
    },
    {
      "section": "api",
      "id": "dotjem.routing.type:transaction",
      "shortName": "transaction",
      "type": "interface",
      "keywords": "an and any api applied applies applying be been cancel cancels causes change changes commit commits committed discarding dotjem final for had have if interface issues it may meaning method of only recieved recorded records routing same series state take that the them to transaction type update updates view views when will"
    }
  ],
  "apis": {
    "api": true
  },
  "html5Mode": false,
  "startPage": "/api",
  "scripts": [
    "angular.min.js"
  ]
};